import { describe, it } from "vitest";

// TODO: updateOrder is not yet implemented in the relayer (returns "not implemented")
describe("updateOrder", () => {
    it.todo("updates min_fill_size on an existing order");
    it.todo("toggles allow_external_matches on an existing order");
});
